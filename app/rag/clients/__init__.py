"""
客户端层

统一封装对外部依赖（Milvus / MongoDB / Neo4j / MinIO / 本地模型 / LLM）的访问。

分层约定：
    - manager/：持有连接或模型实例，负责 init() 幂等建连与 close() 释放；
    - 本层的门面模块（milvus_client.py 等）：只暴露无状态的 get_xxx() 函数，
      供流水线节点调用，避免节点直接依赖 manager 单例的内部字段。

门面函数带「懒加载兜底」：即使 lifespan 未预热（或预热失败），首次调用也会尝试建连，
从而把失败点收敛到真正使用该能力的那一次调用上，而不是让服务整体起不来。
"""
