"""
客户端管理器包

每个 manager 都遵循同一套契约，便于 lifespan 统一按 (名称, 实例) 列表遍历：
    - __init__(config)  ：只保存配置，不做任何 I/O
    - init()            ：幂等建连；已初始化时直接返回
    - close()           ：释放资源；可重复调用

把「建连」与「构造」分离，是为了让模块导入本身保持无副作用——配置类可以在
任何位置安全导入，而真正的网络/显存开销推迟到 lifespan 或首次使用时。
"""
